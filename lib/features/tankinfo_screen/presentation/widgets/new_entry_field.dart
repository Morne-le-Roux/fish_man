import 'package:flutter/material.dart';

class NewEntryField extends StatelessWidget {
  const NewEntryField({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
